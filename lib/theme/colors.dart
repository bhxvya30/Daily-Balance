import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background      = Color(0xFF080808);
  static const surface         = Color(0xFF111111);
  static const surfaceHigh     = Color(0xFF1A1A1A);

  // Glass layers
  static const glassPrimary    = Color(0x0FFFFFFF);
  static const glassSecondary  = Color(0x0AFFFFFF);
  static const glassTertiary   = Color(0x06FFFFFF);

  // Glass borders
  static const borderPrimary   = Color(0x26FFFFFF);
  static const borderSecondary = Color(0x1AFFFFFF);
  static const borderTertiary  = Color(0x0DFFFFFF);

  // Typography
  static const textPrimary     = Color(0xEBFFFFFF);
  static const textSecondary   = Color(0x8CFFFFFF);
  static const textTertiary    = Color(0x59FFFFFF);
  static const textQuaternary  = Color(0x33FFFFFF);

  // Interactive
  static const buttonPrimary   = Color(0xEBFFFFFF);
  static const buttonText      = Color(0xFF000000);
  static const buttonGlass     = Color(0x1AFFFFFF);

  // Ambient orbs
  static const orb1            = Color(0x1FFFFFFF);
  static const orb2            = Color(0x14FFFFFF);
  static const orb3            = Color(0x0DFFFFFF);

  // Chart
  static const barActive       = Color(0xFFFFFFFF);
  static const barInactive     = Color(0x26FFFFFF);

  // Milestone
  static const milestoneUnlocked = Color(0xFFFFFFFF);
  static const milestoneLocked   = Color(0x1AFFFFFF);
}
